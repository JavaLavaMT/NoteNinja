#!/bin/bash
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [ ! -d "$DIR/.venv" ]; then
    echo "Run ./setup.sh first."
    exit 1
fi

source "$DIR/.venv/bin/activate"
cd "$DIR"

if [ "$1" = "pytest" ]; then
    shift
    pytest "$@"
else
    python "$DIR/main.py" "$@"
fi
